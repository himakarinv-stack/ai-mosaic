{
  "name": "ai-mosaic-repo",
  "private": true,
  "description": "Repo tooling for ai-mosaic — husky hooks and commitlint (not published).",
  "scripts": {
    "prepare": "husky",
    "lint:commit": "commitlint --last --verbose",
    "lint:branch": "node scripts/check-branch-name.mjs"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "husky": "^9.1.7"
  },
  "engines": {
    "node": ">=18"
  }
}
